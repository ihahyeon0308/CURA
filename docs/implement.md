구현해.

This is the implementation phase.

All previous restrictions (no implementation) are now lifted.

Requirements:
- No mock data
- All features must be functional
- API must connect to PostgreSQL
- UI must trigger real backend logic

Start with:
1. Database schema (PostgreSQL)
2. NestJS modules (hospitals, search)
3. Next.js data fetching integration

Do not generate placeholder UI.